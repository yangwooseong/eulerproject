# distinct powers

from __future__ import print_function
import math

lists = []

for i in range(2,101):
    for j in range(2,101):
        if pow(i,j) not in lists:
            lists.append(pow(i,j))

print(len(lists))
